{
  "name": "simple-draw-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --watch . --ext ts,js,json --exec ts-node index.ts",
    "build": "echo 'No build needed for JS'",
    "start": "node index.js"
  },
  "engines": {
    "node": ">=20.x"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
